---
platform: linux

image_resource:
  type: docker-image
  source: {repository: maven}

inputs:
- name: resource-tutorial

run:
  path: sh
  args:
  - -exc
  - |
    cd ./resource-tutorial
    cf push
