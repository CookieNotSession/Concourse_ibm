---
platform: linux

image_resource:
  type: docker-image
  source: {repository: maven}

inputs:
- name: resource-tutorial

outputs:
- name: cloudfoundry

run:
  path: sh
  args:
  - -exc
  - |
    cd ./resource-tutorial
    mvn install
    cfpush