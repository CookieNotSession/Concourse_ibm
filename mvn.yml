---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: {ubuntu}

inputs:
- name: resource-tutorial

outputs:
- name: target-web

run:
  path: ./resource-tutorial/cfpush.yml
