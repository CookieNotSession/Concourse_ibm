---
platform: linux

image_resource:
  type: docker-image
  source: {repository: repository: node, tag: "7.5.0"}

inputs:
- name: resource-tutorial

outputs:
- name: target-web

run:
  path: resource-tutorial/mvn.sh
