---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: "8"

inputs:
- name: resource-tutorial

outputs:
- name: target-web

run:
  path: resource-tutorial/mvn.yml
